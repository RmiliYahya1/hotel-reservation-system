<UserControl x:Class="hotel_reservation_desktop_app.ViewModels.GestionRoomTypes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hotel_reservation_desktop_app.ViewModels"
        mc:Ignorable="d"
        Height="450" Width="800">
    <Grid Margin="10,10,10,5">
        <TextBlock
            Text="Gestion des catégories"
            FontSize="22"
            FontWeight="SemiBold"
            Margin="0,7,0,369"
            HorizontalAlignment="Left"
            Foreground="#121518" Width="234"
            />
        <!-- DataGrid pour afficher la liste des roomtypes -->
        <DataGrid Name="RoomTypeDataGrid" AutoGenerateColumns="False" Margin="10,61,10,0" 
               ScrollViewer.HorizontalScrollBarVisibility="Visible" ItemsSource="{Binding RoomTypes}" RenderTransformOrigin="0.5,0.5" Height="353" VerticalAlignment="Top">
            <DataGrid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleX="-0.301"/>
                    <RotateTransform/>
                    <TranslateTransform X="-0.795"/>
                </TransformGroup>
            </DataGrid.RenderTransform>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                <DataGridTextColumn Header="Prix" Binding="{Binding Price}" Width="*" />
                <DataGridTextColumn Header="Capacité" Binding="{Binding Capacity}" Width="*" />
                <DataGridTemplateColumn Header="Opérations" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Modifier" Width="74" Click="ModifierButton_Click" Height="24" Margin="5,5,5,5" 
                                          CommandParameter="{Binding}"/>
                                <Button Content="Supprimer" Width="74" Click="SupprimerButton_Click" Height="24" Margin="5,5,5,5" 
                                          CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Boutons pour les actions -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,3,44,0" Height="38" Width="90">
            <Button Content="Ajouter" Width="74" Click="AjouterButton_Click" Height="24" Margin="5,5,5,5"  />
        </StackPanel>
    </Grid>
</UserControl>
